# shjj-brief 프로젝트 작업 규칙

## 언어 규칙

- 사용자에게 보내는 모든 설명, 진행 메시지, 최종 답변은 한국어로 작성한다.
- 특별한 요청이 없으면 영어로 답변하지 않는다.
- 코드 식별자, 파일명, 라이브러리명, API 이름은 원문을 유지해도 된다.
- 커밋 메시지 제안, 변경 요약, 오류 원인 분석도 한국어로 작성한다.

## 작업 범위 규칙

- 사용자가 명시한 파일 범위를 우선한다.
- 요청받지 않은 코드 변경, 삭제, 이동, 생성은 하지 않는다.
- API 키, 토큰, 비밀번호 등 민감정보를 파일에 추가하지 않는다.
- 외부 서버 전송이 필요한 작업은 사용자가 명시적으로 요청한 경우에만 수행한다.

## 프로젝트 기준

- 이 프로젝트는 정적 PWA 형태의 개인 브리프 앱이다.
- 핵심 화면 파일은 `index.html`, `app.js`, `styles.css`, `sw.js`이다.
- 법령 브리프 데이터는 `data/law_updates.json`을 기준으로 표시한다.
- 감시 법령 목록은 `.github/scripts/watched_laws.json`을 기준으로 관리한다.
- GitHub Actions 워크플로는 `.github/workflows/law-update.yml`에 둔다.

## 검증 규칙

- JSON 파일을 수정한 뒤에는 가능한 경우 `python -m json.tool`로 유효성을 확인한다.
- 앱 동작에 영향을 주는 변경은 브라우저 렌더링 또는 정적 검증 결과를 함께 보고한다.
- 검증을 실행하지 못한 경우에는 이유를 명확히 보고한다.

## 브랜치 운영 규칙

- `main`은 운영본이므로 직접 수정하지 않는다.
- 모든 수정은 `feature/*` 브랜치에서 먼저 진행한다.
- preview에서 사용자가 확인하기 전까지 `main`으로 병합하지 않는다.
- 현재 3차 작업 브랜치는 `feature/v3-law-detail-cards`이다.

## Codex 작업 전 확인

- 작업 전 현재 경로가 실제 프로젝트 폴더인지 확인한다.
- `git branch --show-current`로 현재 브랜치를 확인한다.
- `git status`로 변경 상태를 확인한다.
- `index.html`, `app.js`, `styles.css` 존재 여부를 확인한다.

## 금지 사항

- `data/law_updates.json` 구조를 임의로 바꾸지 않는다.
- `.github/workflows/law-update.yml`은 사용자가 요청하지 않으면 수정하지 않는다.
- 법령 데이터에 없는 변경 내용을 임의 생성하지 않는다.
- 조항별 링크가 없으면 임의로 조항 링크를 만들지 않는다.
- 기존 모바일 카드형 디자인 톤을 유지한다.
