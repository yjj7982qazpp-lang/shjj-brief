# SHJJ Brief 프로젝트 작업 규칙

## 기본 원칙

- 모든 답변, 진행 메시지, 작업 요약은 한국어로 작성한다.
- 설명은 짧고 명확하게 한다.
- 사용자가 바로 복사해서 실행할 수 있는 명령어 중심으로 안내한다.
- 이 프로젝트는 현재 웹/PWA 구조이지만, 향후 iOS 앱과 Android 앱 확장을 고려해 유지한다.
- 단기 작동용 임시 코드보다 유지보수성, 확장성, 비용 효율, 성능, 배포 안정성을 우선한다.
- 대규모 리팩토링은 사용자가 명시적으로 요청한 경우에만 한다.
- 요청 범위 밖 파일 수정, 기능 추가, UI 변경, 구조 변경은 하지 않는다.
- 불필요한 외부 API 호출, 전체 저장소 대량 분석, 반복 브라우저 렌더링은 하지 않는다.
- 보고서는 사용자가 요청하지 않으면 매번 새로 만들지 않는다.
- AI Development Loop 보고서는 하루 1회 기준을 전제로 하고, 중복 이슈와 중복 보고서를 줄이며 전회 대비 발전점을 우선 표시한다.

## 브랜치 운영 원칙

- `main`은 운영본이다. 직접 수정하지 않는다.
- 새 기능, UI 개선, 실험성 변경은 `preview` 또는 `feature/*` 브랜치에서 작업한다.
- 사용자가 preview에서 확인하고 승인하기 전까지 `main`으로 병합하지 않는다.
- 운영본 화면에는 `Preview`, `Pre`, `v0.x`, `v4` 같은 검수용 문구가 절대 노출되면 안 된다.
- `main`의 화면 제목은 항상 `SHJJ Brief`로 유지한다.
- `preview`의 화면 제목은 `SHJJ Brief Preview v0.x` 형식으로 관리한다.

## 작업 시작 전 확인

- 반드시 실제 Git 저장소인지 먼저 확인한다.
- 항상 아래 5가지를 먼저 확인한다.
- `Get-Location`
- `git rev-parse --is-inside-work-tree`
- `git branch --show-current`
- `git status --short --branch`
- `Get-ChildItem app.js,index.html,styles.css`

## 작업 범위 원칙

- 이번 프로젝트의 실제 작업 폴더는 `Desktop/shjj-brief`이다.
- Codex 샌드박스용 빈 폴더를 기준으로 작업하지 않는다.
- 사용자가 요청한 범위만 수정한다.
- 큰 변경보다 작은 변경을 우선한다.
- 요청받지 않은 파일 생성, 삭제, 이동, 전체 재작성은 하지 않는다.
- `app.js`, `styles.css`, `index.html`, `sw.js`, `data/*`는 사용자가 명시적으로 요청한 경우에만 수정한다.
- GitHub Actions 관련 파일은 사용자가 요청하지 않으면 수정하지 않는다.
- `git push`, `main` 병합, 배포, 대량 삭제, 프로젝트 외부 파일 수정은 사용자 확인 없이 하지 않는다.

## 인코딩 원칙

- 모든 텍스트 파일은 UTF-8 기준으로 저장한다.
- 작업 전 `index.html`의 charset을 확인한다.
- `index.html`에는 `<meta charset="UTF-8" />`가 유지되어야 한다.
- 한글 깨짐 방지를 최우선으로 한다.
- Windows PowerShell의 단순 `Set-Content`, `-replace`로 한글 포함 파일을 직접 수정하지 않는다.
- 텍스트 파일 수정 자동화가 필요하면 Python을 사용하고 `encoding="utf-8"`을 명시한다.
- `.editorconfig`, `.gitattributes`가 있으면 그 규칙을 우선 따른다.

## 프로젝트 기준 정보

- 핵심 화면 파일은 `index.html`, `app.js`, `styles.css`, `sw.js`이다.
- 법령 브리프 데이터는 `data/law_updates.json`을 기준으로 표시한다.
- 감시 법령 목록은 `.github/scripts/watched_laws.json`을 기준으로 관리한다.
- GitHub Actions 워크플로는 `.github/workflows/law-update.yml`에 둔다.
- 법령 브리프의 핵심은 "오늘 시행/변경된 내용이 모바일에서 읽기 쉽게 보이는 것"이다.
- AI 요약은 원문 복붙보다 실무자가 빠르게 이해할 수 있는 요약을 우선한다.

## 유지해야 할 핵심 기능

- 법령 자동 업데이트 구조 유지
- `metadata/items` 구조와 구형 배열 JSON 호환 유지
- 오늘/7일/30일 탭 유지
- 법령 카드 접기/펼치기 유지
- 오늘날씨 표시 유지
- 오늘 행동 가이드 유지
- 오늘일정 접기/펼치기 유지
- 하단 네비 유지
- 알림 테스트 버튼 유지
- 알림 시간 설정 UI와 `localStorage` 저장 기능 유지

## UI 원칙

- 모바일 가독성을 우선한다.
- 기존 `main`/`preview`의 흰색 카드, 둥근 모서리, 부드러운 그림자 톤을 유지한다.
- 첫 화면은 복잡하지 않게 유지한다.
- 자주 안 쓰는 설정은 접기/펼치기로 숨긴다.
- 알림 시간 설정은 상단 작은 버튼 형태로 두고, 누르면 설정 카드가 펼쳐지는 구조를 우선한다.
- 기존 디자인 톤을 새 디자인으로 갈아엎지 않는다.

## 데이터 및 법령 표시 원칙

- `data/law_updates.json` 구조를 임의로 바꾸지 않는다.
- 법령 데이터에 없는 변경 내용을 임의 생성하지 않는다.
- 조항별 링크가 없으면 링크를 임의 생성하지 않는다.
- 사용자가 가장 원하는 정보는 "오늘 또는 최근 변경된 법령에서 실제로 무엇이 바뀌었는지"이다.
- 시행일과 공포일은 중요하므로 유지한다.
- 기준일, 확인상태, 변경기준, 관련성메모 같은 보조 배지는 과하게 노출하지 않는다.
- 변경 내용은 `data/law_updates.json`에 실제 필드가 있을 때만 표시한다.
- 데이터에 없는 내용을 추정해서 만들지 않는다.

## AI 요약 및 캐시 원칙

- 법령 AI 요약은 `.github/scripts/collect_law_updates.py` 실행 시에만 생성한다.
- `summary_key`는 법령명, 시행일, 공포일, 개정문, 변경 이유, 관련 조문을 정규화해 SHA256으로 만든다.
- 같은 `summary_key`는 `data/law_summary_cache.json`의 기존 요약을 재사용한다.
- `OPENAI_API_KEY`가 없어도 전체 수집이 실패하지 않도록 fallback 요약을 사용한다.

## 테스트 원칙

- 수정 후 가능한 경우 아래만 우선 실행한다.
- `node --check app.js`
- `python -m py_compile .github/scripts/collect_law_updates.py`
- 깨진 문자열 검색: `�`, `??/span>`, `??/div>`, `??/strong>`
- JSON 파일을 수정했을 때만 가능한 경우 `python -m json.tool`로 유효성을 확인한다.
- 테스트가 불가능하면 불가능한 이유를 짧게 적는다.
- 검증을 실행하지 못했는데 성공으로 보고하지 않는다.

## Codex 작업 규칙

- 작업 전후로 변경 파일을 명확히 구분해 보고한다.
- 불확실하면 임의로 만들지 말고 현재 파일에서 확인한 사실만 기준으로 작업한다.
- 실수 방지를 위해 먼저 파일 존재 여부와 브랜치 상태를 확인한 뒤 수정한다.
- 코드 수정 시 한글 인코딩 보존을 우선한다.
- 앱 구조는 향후 API 연동, 상태 관리, 알림 설정, UI 컴포넌트 분리가 가능하도록 유지한다.

## 금지 사항

- `main` 직접 수정 금지
- 사용자가 요청하지 않은 새 라이브러리 추가 금지
- 사용자가 요청하지 않은 전체 코드 재작성 금지
- 사용자가 요청하지 않은 GitHub Actions 수정 금지
- 사용자가 요청하지 않은 법령 데이터 구조 변경 금지
- 운영본에 preview 문구 노출 금지

## 완료 보고 형식

- 작업 완료 후 아래만 짧게 보고한다.
- 수정 파일 목록
- 변경 내용
- 유지한 기능
- 테스트 결과
- preview에서 확인할 항목
- main 승격 전 주의사항
